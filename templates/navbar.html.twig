<nav class="navbar">
  <a href="#" class="sidebar-toggler">
    <i data-feather="menu"></i>
  </a>
  <div class="navbar-content">
    {% if app.user %}
      <ul class="navbar-nav">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="profileDropdown" role="button" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <i data-feather="user" class="wd-30 ht-30 rounded-circle"></i> {{ app.user.email }}
          </a>
          <div class="dropdown-menu p-0" aria-labelledby="profileDropdown">
            <ul class="list-unstyled p-1">
              <li class="dropdown-item py-2">
                  {# TODO: routes app_user_edit et app_user_profil à recréer #}
                  {# {% if is_granted('ROLE_ADMIN') %}
                    <a href="{{ path('app_user_edit', {'id': app.user.id}) }}" class="text-body ms-0">
                  {% else %}
                    <a href="{{ path('app_user_profil') }}" class="text-body ms-0">
                  {% endif %}
                  <i class="me-2 icon-md" data-feather="edit"></i>
                  <span>Profil</span>
                  </a> #}
              </li>
              <li class="dropdown-item py-2">
                  <a href="{{ path('app_logout') }}" class="text-body ms-0">
                  <i class="me-2 icon-md" data-feather="log-out"></i>
                  <span>Déconnexion</span>
                  </a>
              </li>
            </ul>
          </div>
        </li>
      </ul>
    {% endif %}
  </div>
</nav>